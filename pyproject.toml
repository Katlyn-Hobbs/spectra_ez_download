[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "spectra_ez_download"
version = "0.0.1"

dependencies = [
    "numpy",
    "astropy",
    "pandas",
    "matplotlib",
    "scipy",
]